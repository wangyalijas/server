module.exports = {
    Add: class Add {
        constructor(workId, tagId, workType) {
            this.workId = workId;
            this.tagId = tagId;
            this.workType = workType;
        }
    }
}